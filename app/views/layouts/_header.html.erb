
<nav class="navbar navbar-expand-lg navbar-light bg-light">

<div class="container-fluid">
  <%= link_to 'Task_Management' , root_path, class: "navbar-brand" %>
   <!-- <button class="navbar-toggler" type="button" data-bs-toggle="false" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button> -->
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
      <li class="nav-item">
      <%= link_to 'Task registration' ,new_task_path, class: "nav-link" %>
    </li>

    <li class="nav-item">
      <%= link_to 'Task list' , tasks_path, class: "nav-link" %>
    </li>
      <li class="nav-item">
      <% if logged_in? %>
      <% if current_user.admin? %>
      <a class="nav-link" href="#"><%= link_to 'App Users', admin_users_path, class: "nav-link" %></a>
   <%end %>
   <%end %>
    </ul>
  </div>
</div>

<div class="collapse navbar-collapse" id="navbarSupportedContent">
<ul class="navbar-nav me-auto mb-2 mb-lg-0">

<% if logged_in? %>
<li class="nav-item">
      <a class="nav-link"><%= "Admin" if current_user.admin? %></a>
      <%= link_to current_user.name, user_path(current_user.id),class: "nav-link" %>
      <%= link_to "Logout", session_path(current_user.id),class: "nav-link", method: :delete %>

     <% else %>

      <%= link_to "Sign up", new_user_path, class: "nav-link" %>
      <%= link_to "Sign in", new_session_path,class: "nav-link" %>

    <% end %>
</li>
</ul>
</div>
</nav>
<% flash.each do |key, value| %>
<%= content_tag(:div, value, class: "#{key}") %>
<% end %>
